<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE hibernate-configuration PUBLIC 
    "-//Hibernate/Hibernate Configuration DTD 3.0//EN" 
    "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- PostgreSQL JDBC driver -->
        <property name="hibernate.connection.driver_class">org.postgresql.Driver</property>

        <!-- PostgreSQL connection URL from Render -->
        <property name="hibernate.connection.url">jdbc:postgresql://dpg-dipdcser433s73d9h1q9-a:5432/hibermain_db</property>

        <!-- Credentials from Render -->
        <property name="hibernate.connection.username">hibermain_db_user</property>
        <property name="hibernate.connection.password">F7Dcaqs6XaM5Bd5T0unpjVX8uLpzOJbX</property>

        <!-- PostgreSQL-specific dialect -->
        <property name="hibernate.dialect">org.hibernate.dialect.PostgreSQLDialect</property>

        <!-- Logging and table auto-creation -->
        <property name="show_sql">true</property>
        <property name="format_sql">true</property>
        <property name="hbm2ddl.auto">update</property>

        <!-- Hibernate entity class -->
        <mapping class="hibermainproject.Blog"/>
    </session-factory>
</hibernate-configuration>
